{
  "name": "appointy-fullstack",
  "private": true,
  "scripts": {
    "build": "npm --prefix backend install && npm --prefix frontend install && npm --prefix admin install && npm --prefix frontend run build && npm --prefix admin run build",
    "start": "npm --prefix backend start"
  }
}
